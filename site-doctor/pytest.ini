[pytest]
# site-doctor/ is the import root: the code uses absolute imports like
# `from models.schemas import Issue`, so pytest needs this directory on
# sys.path. Without it every test fails at collection with ImportError.
pythonpath = .
testpaths = tests
